# P0 实施与验收记录（2026-09-24）

范围来自《插件评估发展建议》的两项 P0：原生接入闭环，以及一个真实外部插件的诊断、修复、完整复测。候选由用户指定为 NGS Analysis 和 BioNexus。PVL 版本保持 0.5.0。

工程验收：`python -B scripts/check_release_tests.py` **421 项通过、0 失败、0 错误、0 跳过**。生成插件副本逐字节检查、Agent Plugins 1.0.0 清单验证及 diff 空白检查通过。这是当前本机验证，没有新增远端 CI、发布或安装验收。历史 `VERIFICATION.json.tests` 保留，新增 P0 验收单列记录。

## 原生接入

新增 `prepare-native-evidence`、`capture-native-evidence`、`verify-native-evidence`。复用原生用例，附加明确文件映射和独立科研评分；保存选定用例/插件字节、版本、输入、输出、原生结果和可获得的事件。每项评分可定位到收集文件和规则摘要，离线迁移后可重算。

不导入猜测的会话身份、不补造缺失输出、不把原生数组位置当作随机配对、不因单个文件正确就升级为整体插件价值。保留原生失败和可观察文件的局部诊断，费用估计与实际结算分开。评分参考不进入被测插件；路径检查不能替代执行环境的访问隔离。

使用步骤见 [原生科研文件附加诊断](NATIVE-EVIDENCE.zh-CN.md)。新增回归覆盖原用例/参考/插件篡改、目录越界、重复运行/会话/目录、缺失运行与产物、有效行顺序变化、错误数值、离线迁移及 CLI 全流程。

## 真实原生 CSV 验收

用户授权后，使用当前配置的 DeepSeek `deepseek-flash`，在 Claude Code 2.1.278 上执行公开三行 CSV，一次 WITH、一次 WITHOUT。候选是从 NGS 0.2.16 原样复制的 `understand-ngs-results` 技能封装，明确不是完整安装包；没有运行 NGS 管线、真实 MCP、shell 或发布报告。

首次尝试被当前工具沙箱的网络权限阻断，两臂各在 90 秒后终止；保留原始结果与仅含 init/API retry 的日志，费用结算未知。确认父进程已结束及本地连接出现 WinError 10013 后，在已授权网络环境中对同一冻结用例另存重跑，13.76 秒完成两臂。旧记录没有覆盖或改写为成功。

官方 `file_exists` 两臂都通过。PVL 从宿主 `tracePath` 和 `init.cwd` 精确关联产物后发现：

| 检查 | WITH | WITHOUT |
| --- | --- | --- |
| 三基因完整检验集合及 BH 重算 | 通过：0.03、0.04、0.04 | 失败：0.03、0.045、0.045 |
| 原始 p 值与效应数值保留 | 通过 | 通过 |
| Skill 调用 | 未观察到 | 未观察到 |

SciPy 独立计算同样得到 0.03、0.04、0.04。**这验证了附加诊断能指出实际数值错误；一个案例、每臂一次、且未观察到技能调用，不能支持插件增益归因。** 失败包与成功包均离线重算；成功包迁移目录后再次得到 `REPRODUCED`。

自动运行映射暂限实测 Claude Code 2.1.278；其他版本仍须显式映射。原生 Windows 明确警告无法完成保留目录权限封存。本次参考文件位于插件和运行目录之外，没有传给模型，但**没有证明它在 OS 层不可读取/改写**。因此 P0 的严格参考隔离验收仍有缺口，不能声称已实现完整盲法或不可篡改宿主证据。

费用分开保留：原生自定义模型计价标记 `costBasis=unknown`，报告为 US$0.111179，第二次在途运行使 US$0.10 估算阈值略超；根据[DeepSeek 官方峰时费率](https://api-docs.deepseek.com/quick_start/pricing/)与两份原生 token 台账逐项较大值，提供方费用估算为 **US$0.005986428**。二者都不是实际结算；首次网络失败及总账结算保持未知。付费目录调用为零。

机器摘要：[原生 CSV 执行与回放收据](evidence/p0-native-csv-20260924.json)。完整本地材料：`C:\Test\p0-native-csv-20260924`；迁移重放：`C:\Test\p0-native-csv-replay-20260924`。

## 真实组件修复

原始文件从当前安装/工作区读取并保存原始字节摘要；没有人为给插件注入缺陷。测试输入为公开披露的合成对照，不是生物样本、独立保留集或盲测。所有用例在本轮前后执行之前冻结，同一完整列表分别运行两版，正常对照也保留。

| 候选与执行范围 | 真实发现 | 修复前 | 修复后 |
| --- | --- | --- | --- |
| NGS Analysis Workbench 0.2.16，完整执行复制的 `aggregate_salmon_quant.py` | `quant.sf` 的重复转录本被字典构造静默覆盖；非有限/负计数等无效输入可进入输出 | 2/7 | 7/7 |
| BioNexus 当前工作区，精确执行 `parse_samplesheet` 函数 AST | 组内重复按表格行计数，供体缺失时以行数代替生物重复数 | 2/5 | 5/5 |

NGS 修复在矩阵写入之前拒绝重复/空转录本 ID、重复或缺失必要列、空表、不完整行、非有限/负数值；保留分数计数和零计数的有效输入。它没有重跑 FASTQ、Salmon 或整条工作流。

BioNexus 修复按声明的供体在各条件内去重；供体字段不完整时，返回明确 `UNRESOLVED` 并使计数不可作为重复充足性的证据。当前 `harvest_nextflow_run` 主链已不调用这个辅助函数，所以本次不能宣称发现或修复了主链科学证据泄漏。BioNexus 工作区有大量未提交修改；本次只修复隔离副本并交付补丁，没有覆盖现有工作，也未验证完整包集成。

复现入口：

```sh
python scripts/run_p0_repair_case.py --ngs-source PATH_TO_AGGREGATE_SALMON_QUANT.py --bionexus-source PATH_TO_NEXTFLOW_BRIDGE.py --output NEW_DIRECTORY
```

脚本拒绝未知源形态和复用输出目录。输出包含原版/修订版源文件、精确补丁、冻结协议、每次输入/产物/日志、PVL 内置产物评分、投入时间缺口和摘要清单。它执行选定本地组件，不能作为模型或官方宿主执行收据。

机器摘要见 [组件修复证据](evidence/p0-component-repair-20260924.json)。完整本地材料：`C:\Test\p0-repair-20260924-r2`。最初一轮完整保留在 `C:\Test\p0-repair-20260924`；r2 增加显式供体未知状态后重新冻结、完整重跑，未覆盖旧记录。

## 人的参与、投入与验收边界

用户在看到两项具体诊断后回复“采纳两项诊断及修复方向”。按原话记录为当前用户的反馈，不推定其上游作者身份，不写成独立作者认可。参见 [实际反馈记录](evidence/p0-user-feedback-20260924.json)。尚未收到负面反馈、人工投入分钟数、误诊复核或下一真实问题的主动复用；均保留 null，不能填成零。

已达到：原生附加文件诊断的实现、真实模型文件采集与独立重算；两个真实组件缺陷的复现、修复、完整本地复测；当前用户对诊断和修复方向的实际采纳。

仍需独立验收：运行时评分参考的强访问隔离、上游维护者在其环境应用修复并反馈、完整插件/工作流复测、PVL 对人工时间和修复质量的增益。不能将这些本地记录写成“全部 P0 验收已完成”“独立外部作者采用闭环已完成”或“科研收益已证明”。
